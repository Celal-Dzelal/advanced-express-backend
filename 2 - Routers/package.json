{
  "name": "2---routers",
  "version": "1.0.0",
  "description": "```\r npm init -y                   # Create package.json (initialize project)\r npm install -D nodemon        # Install nodemon as a dev dependency\r npm install express dotenv    # Install express and dotenv\r echo PORT=8000 > .env         # Create .env file and set PORT\r ```",
  "main": "routers.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^3.1.9"
  },
  "dependencies": {
    "and": "^0.0.3",
    "dotenv": "^16.4.7",
    "express": "^5.1.0"
  }
}
